---
<<<<<<< HEAD
import { classifyRegion, getRegionBySlug } from '../utils/region';

const { entry } = Astro.props as { entry: any };

// Classify region from title + body
const regionSlug = classifyRegion(
  entry.data.title,
  entry.body || '',
  entry.data.tags || []
);
const regionInfo = getRegionBySlug(regionSlug);
const regionLabel = regionInfo?.name || entry.data.category || '';
---

<a href={`/${entry.id}/`} class="card-link block bg-white rounded-xl shadow-sm overflow-hidden hover:shadow-md transition-shadow border border-primary/10">
  {
    entry.data.heroImage ? (
      <img
        src={entry.data.heroImage}
        alt={entry.data.title}
        class="w-full h-48 object-cover"
        loading="lazy"
      />
    ) : (
      <div class="w-full h-48 bg-slate-200 flex items-center justify-center text-slate-400 text-sm">无图</div>
    )
  }
  <div class="p-5">
    <div class="flex items-center gap-2 mb-2">
      <span class="text-xs font-bold uppercase text-accent bg-accent/10 px-2 py-0.5 rounded">{regionLabel}</span>
    </div>
    <h3 class="font-heading text-lg text-primary line-clamp-2 leading-snug">{entry.data.title}</h3>
=======
const { entry } = Astro.props as { entry: any };
const heroImg = entry.data.heroImage || entry.data.ogImage;
---

<a href={`/${entry.id}/`} class="card-link group bg-white rounded-2xl overflow-hidden shadow-sm hover:shadow-lg transition-all duration-300 border border-slate-100">
  {heroImg ? (
    <div class="aspect-[16/9] overflow-hidden bg-slate-100">
      <img src={heroImg} alt={entry.data.title} class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500" loading="lazy" />
    </div>
  ) : (
    <div class="aspect-[16/9] bg-gradient-to-br from-teal-50 to-amber-50 flex items-center justify-center">
      <span class="text-4xl">🏠</span>
    </div>
  )}
  <div class="p-5">
    <span class="text-xs font-semibold uppercase tracking-wider text-accent">{entry.data.category}</span>
    <h3 class="font-heading text-lg font-semibold text-ink mt-2 leading-snug group-hover:text-primary transition-colors">{entry.data.title}</h3>
>>>>>>> 3299e1a (重设计: 留学生公寓 现代清新视觉 + heroImage配图 + 全站部署)
    <p class="text-sm text-muted mt-2 line-clamp-2">{entry.data.description}</p>
  </div>
</a>
